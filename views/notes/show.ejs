<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        ::-webkit-scrollbar {
            width: 7px;
        }

        ::-webkit-scrollbar-track {
            background: rgb(118, 0, 0);
        }

        ::-webkit-scrollbar-thumb {
            background-color: black;
        }

        h1,
        h2,
        h3,
        h4,
        h5,
        div,
        p {
            color: rgb(189, 187, 187) !important;
            /*opacity: .8;*/
        }

        .card {
            background-image: linear-gradient(to right, #434343 0%, black 100%);
        }

        .background-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: .5;
            filter: blur(10px);

        }

        .background-image {
            width: 100%;
            height: 100%;
            background: url('https://github.com/raybags-web-dev/image_base/blob/master/images/background/uu.jpeg?raw=true') no-repeat center center fixed;
            background-size: cover;
        }
    </style>
</head>

<body class="bg-dark">
    <div class="background-container">
        <div class="background-image"></div>
    </div>
    <div class="container pt-3">
        <h3 class="mb-4">
            <%= note.title %>
        </h3>

        <div class="text-muted mb-2">
            <%= note.createdAt.toLocaleDateString() %>
        </div>

        <a href="/" class="btn btn-secondary bg-transparent shadow-sm">All notes</a>
        <a href="/notes/edit/<%= note.id %>" class="btn bg-transparent btn-info text-white shadow-sm">Edit note</a>
        <hr>
        <div class="card-text mb-2  p-3">
            <%= note.description %>
        </div>
        <hr>
        <div class="d-inline p-3">
            <%- note.sanitizedHtml %>
        </div>
    </div>
</body>

</html>